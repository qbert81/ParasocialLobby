--!strict
--!native
--!optimize 2
--!nolint LocalShadow
--#selene: allow(shadowing)
-- File generated by Blink v0.15.4 (https://github.com/1Axen/Blink)
-- This file is not meant to be edited

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")

local Invocations = 0

local SendSize = 64
local SendOffset = 0
local SendCursor = 0
local SendBuffer = buffer.create(64)
local SendInstances = {}

local RecieveCursor = 0
local RecieveBuffer = buffer.create(64)

local RecieveInstances = {}
local RecieveInstanceCursor = 0

type Entry = {
    value: any,
    next: Entry?
}

type Queue = {
    head: Entry?,
    tail: Entry?
}

type BufferSave = {
    Size: number, 
    Cursor: number, 
    Buffer: buffer, 
    Instances: {Instance}
}

local function Read(Bytes: number)
    local Offset = RecieveCursor
    RecieveCursor += Bytes
    return Offset
end

local function Save(): BufferSave
    return {
        Size = SendSize,
        Cursor = SendCursor,
        Buffer = SendBuffer,
        Instances = SendInstances
    }
end

local function Load(Save: BufferSave?)
    if Save then
        SendSize = Save.Size
        SendCursor = Save.Cursor
        SendOffset = Save.Cursor
        SendBuffer = Save.Buffer
        SendInstances = Save.Instances
        return
    end

    SendSize = 64
    SendCursor = 0
    SendOffset = 0
    SendBuffer = buffer.create(64)
    SendInstances = {}
end

local function Invoke()
    if Invocations == 255 then
        Invocations = 0
    end

    local Invocation = Invocations
    Invocations += 1
    return Invocation
end

local function Allocate(Bytes: number)
    local InUse = (SendCursor + Bytes)
    if InUse > SendSize then
        --> Avoid resizing the buffer for every write
        while InUse > SendSize do
            SendSize *= 1.5
        end

        local Buffer = buffer.create(SendSize)
        buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
        SendBuffer = Buffer
    end

    SendOffset = SendCursor
    SendCursor += Bytes
    
    return SendOffset
end

local function CreateQueue(): Queue
    return {
        head = nil,
        tail = nil
    }
end

local function Pop(queue: Queue): any
    local head = queue.head
    if head == nil then
        return
    end

    queue.head = head.next
    return head.value
end

local function Push(queue: Queue, value: any)
    local entry: Entry = {
        value = value,
        next = nil
    }

    if queue.tail ~= nil then
        queue.tail.next = entry
    end

    queue.tail = entry

    if queue.head == nil then
        queue.head = entry
    end
end

local Calls = table.create(256)

local Events: any = {
    Reliable = table.create(256),
    Unreliable = table.create(256)
}

local Queue: any = {
    Reliable = table.create(256),
    Unreliable = table.create(256)
}

Queue.Reliable[0] = table.create(256)
Queue.Reliable[1] = table.create(256)
Queue.Reliable[2] = table.create(256)
export type ItemMap = {[number]: boolean}
export type EquippedMap = {[number]: number}
export type PlayerData = { Gadgets: {[number]: boolean}, EquippedGadgets: {[number]: number}, Emotes: {[number]: boolean}, EquippedEmotes: {[number]: number}, Bits: number, Wins: number, FastestWin: number, Level: number, Xp: number, GamepassDictionary: {[string]: boolean}, DeveloperProductPurchases: {[string]: number} }
local function WriteFUNCTION_DATA_FetchPlayerData(Value: nil, InvocationIdentifier: number): ()
	-- Allocate BLOCK: 2 bytes
	local BLOCK_START = Allocate(2)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 0)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, InvocationIdentifier)
end

local function ReadFUNCTION_RETURN_FetchPlayerData(): ({ Gadgets: {[number]: boolean}, EquippedGadgets: {[number]: number}, Emotes: {[number]: boolean}, EquippedEmotes: {[number]: number}, Bits: number, Wins: number, FastestWin: number, Level: number, Xp: number, GamepassDictionary: {[string]: boolean}, DeveloperProductPurchases: {[string]: number} }?)
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	local Success = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	if Success ~= 1 then
		error("Server encountered an error.")
	end
	local Value
	if buffer.readu8(RecieveBuffer, BLOCK_START + 1) == 1 then
		-- Read BLOCK: 32 bytes
		local BLOCK_START = Read(32)
		Value = {} :: any
		Value.Gadgets = {}
		local Elements_2 = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
		for _ = 1, Elements_2 do
			local OFFSET_0 = Read(1)
			local Key_2 = buffer.readu8(RecieveBuffer, OFFSET_0)
			local OFFSET_1 = Read(1)
			local Element_2 = (buffer.readu8(RecieveBuffer, OFFSET_1) == 1)
			Value.Gadgets[Key_2] = Element_2
		end
		Value.EquippedGadgets = {}
		Elements_2 = buffer.readu16(RecieveBuffer, BLOCK_START + 2)
		for _ = 1, Elements_2 do
			local OFFSET_0 = Read(1)
			local Key_2 = buffer.readu8(RecieveBuffer, OFFSET_0)
			local OFFSET_1 = Read(1)
			local Element_2 = buffer.readu8(RecieveBuffer, OFFSET_1)
			Value.EquippedGadgets[Key_2] = Element_2
		end
		Value.Emotes = {}
		Elements_2 = buffer.readu16(RecieveBuffer, BLOCK_START + 4)
		for _ = 1, Elements_2 do
			local OFFSET_0 = Read(1)
			local Key_2 = buffer.readu8(RecieveBuffer, OFFSET_0)
			local OFFSET_1 = Read(1)
			local Element_2 = (buffer.readu8(RecieveBuffer, OFFSET_1) == 1)
			Value.Emotes[Key_2] = Element_2
		end
		Value.EquippedEmotes = {}
		Elements_2 = buffer.readu16(RecieveBuffer, BLOCK_START + 6)
		for _ = 1, Elements_2 do
			local OFFSET_0 = Read(1)
			local Key_2 = buffer.readu8(RecieveBuffer, OFFSET_0)
			local OFFSET_1 = Read(1)
			local Element_2 = buffer.readu8(RecieveBuffer, OFFSET_1)
			Value.EquippedEmotes[Key_2] = Element_2
		end
		Value.Bits = buffer.readu32(RecieveBuffer, BLOCK_START + 8)
		Value.Wins = buffer.readu16(RecieveBuffer, BLOCK_START + 12)
		Value.FastestWin = buffer.readf64(RecieveBuffer, BLOCK_START + 14)
		Value.Level = buffer.readu16(RecieveBuffer, BLOCK_START + 22)
		Value.Xp = buffer.readu32(RecieveBuffer, BLOCK_START + 24)
		Value.GamepassDictionary = {}
		Elements_2 = buffer.readu16(RecieveBuffer, BLOCK_START + 28)
		for _ = 1, Elements_2 do
			local OFFSET_0 = Read(2)
			local Length = buffer.readu16(RecieveBuffer, OFFSET_0)
			local Key_2 = buffer.readstring(RecieveBuffer, Read(Length), Length)
			local OFFSET_1 = Read(1)
			local Element_2 = (buffer.readu8(RecieveBuffer, OFFSET_1) == 1)
			Value.GamepassDictionary[Key_2] = Element_2
		end
		Value.DeveloperProductPurchases = {}
		Elements_2 = buffer.readu16(RecieveBuffer, BLOCK_START + 30)
		for _ = 1, Elements_2 do
			local OFFSET_0 = Read(2)
			local Length = buffer.readu16(RecieveBuffer, OFFSET_0)
			local Key_2 = buffer.readstring(RecieveBuffer, Read(Length), Length)
			local OFFSET_1 = Read(2)
			local Element_2 = buffer.readu16(RecieveBuffer, OFFSET_1)
			Value.DeveloperProductPurchases[Key_2] = Element_2
		end
	end
	return Value
end

local function WriteFUNCTION_DATA_PurchaseInventoryItem(Value1: number, Value2: string, InvocationIdentifier: number): ()
	-- Allocate BLOCK: 5 bytes
	local BLOCK_START = Allocate(5)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 1)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, InvocationIdentifier)
	buffer.writeu8(SendBuffer, BLOCK_START + 2, Value1)
	local Length = #Value2
	buffer.writeu16(SendBuffer, BLOCK_START + 3, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value2, Length)
end

local function ReadFUNCTION_RETURN_PurchaseInventoryItem(): ({ Gadgets: {[number]: boolean}, EquippedGadgets: {[number]: number}, Emotes: {[number]: boolean}, EquippedEmotes: {[number]: number}, Bits: number, Wins: number, FastestWin: number, Level: number, Xp: number, GamepassDictionary: {[string]: boolean}, DeveloperProductPurchases: {[string]: number} }?)
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	local Success = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	if Success ~= 1 then
		error("Server encountered an error.")
	end
	local Value
	if buffer.readu8(RecieveBuffer, BLOCK_START + 1) == 1 then
		-- Read BLOCK: 32 bytes
		local BLOCK_START = Read(32)
		Value = {} :: any
		Value.Gadgets = {}
		local Elements_2 = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
		for _ = 1, Elements_2 do
			local OFFSET_0 = Read(1)
			local Key_2 = buffer.readu8(RecieveBuffer, OFFSET_0)
			local OFFSET_1 = Read(1)
			local Element_2 = (buffer.readu8(RecieveBuffer, OFFSET_1) == 1)
			Value.Gadgets[Key_2] = Element_2
		end
		Value.EquippedGadgets = {}
		Elements_2 = buffer.readu16(RecieveBuffer, BLOCK_START + 2)
		for _ = 1, Elements_2 do
			local OFFSET_0 = Read(1)
			local Key_2 = buffer.readu8(RecieveBuffer, OFFSET_0)
			local OFFSET_1 = Read(1)
			local Element_2 = buffer.readu8(RecieveBuffer, OFFSET_1)
			Value.EquippedGadgets[Key_2] = Element_2
		end
		Value.Emotes = {}
		Elements_2 = buffer.readu16(RecieveBuffer, BLOCK_START + 4)
		for _ = 1, Elements_2 do
			local OFFSET_0 = Read(1)
			local Key_2 = buffer.readu8(RecieveBuffer, OFFSET_0)
			local OFFSET_1 = Read(1)
			local Element_2 = (buffer.readu8(RecieveBuffer, OFFSET_1) == 1)
			Value.Emotes[Key_2] = Element_2
		end
		Value.EquippedEmotes = {}
		Elements_2 = buffer.readu16(RecieveBuffer, BLOCK_START + 6)
		for _ = 1, Elements_2 do
			local OFFSET_0 = Read(1)
			local Key_2 = buffer.readu8(RecieveBuffer, OFFSET_0)
			local OFFSET_1 = Read(1)
			local Element_2 = buffer.readu8(RecieveBuffer, OFFSET_1)
			Value.EquippedEmotes[Key_2] = Element_2
		end
		Value.Bits = buffer.readu32(RecieveBuffer, BLOCK_START + 8)
		Value.Wins = buffer.readu16(RecieveBuffer, BLOCK_START + 12)
		Value.FastestWin = buffer.readf64(RecieveBuffer, BLOCK_START + 14)
		Value.Level = buffer.readu16(RecieveBuffer, BLOCK_START + 22)
		Value.Xp = buffer.readu32(RecieveBuffer, BLOCK_START + 24)
		Value.GamepassDictionary = {}
		Elements_2 = buffer.readu16(RecieveBuffer, BLOCK_START + 28)
		for _ = 1, Elements_2 do
			local OFFSET_0 = Read(2)
			local Length = buffer.readu16(RecieveBuffer, OFFSET_0)
			local Key_2 = buffer.readstring(RecieveBuffer, Read(Length), Length)
			local OFFSET_1 = Read(1)
			local Element_2 = (buffer.readu8(RecieveBuffer, OFFSET_1) == 1)
			Value.GamepassDictionary[Key_2] = Element_2
		end
		Value.DeveloperProductPurchases = {}
		Elements_2 = buffer.readu16(RecieveBuffer, BLOCK_START + 30)
		for _ = 1, Elements_2 do
			local OFFSET_0 = Read(2)
			local Length = buffer.readu16(RecieveBuffer, OFFSET_0)
			local Key_2 = buffer.readstring(RecieveBuffer, Read(Length), Length)
			local OFFSET_1 = Read(2)
			local Element_2 = buffer.readu16(RecieveBuffer, OFFSET_1)
			Value.DeveloperProductPurchases[Key_2] = Element_2
		end
	end
	return Value
end

local function WriteFUNCTION_DATA_EquipItemAtIndex(Value1: number, Value2: number, Value3: string, InvocationIdentifier: number): ()
	-- Allocate BLOCK: 6 bytes
	local BLOCK_START = Allocate(6)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 2)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, InvocationIdentifier)
	buffer.writeu8(SendBuffer, BLOCK_START + 2, Value1)
	buffer.writeu8(SendBuffer, BLOCK_START + 3, Value2)
	local Length = #Value3
	buffer.writeu16(SendBuffer, BLOCK_START + 4, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value3, Length)
end

local function ReadFUNCTION_RETURN_EquipItemAtIndex(): ({ Gadgets: {[number]: boolean}, EquippedGadgets: {[number]: number}, Emotes: {[number]: boolean}, EquippedEmotes: {[number]: number}, Bits: number, Wins: number, FastestWin: number, Level: number, Xp: number, GamepassDictionary: {[string]: boolean}, DeveloperProductPurchases: {[string]: number} }?)
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	local Success = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	if Success ~= 1 then
		error("Server encountered an error.")
	end
	local Value
	if buffer.readu8(RecieveBuffer, BLOCK_START + 1) == 1 then
		-- Read BLOCK: 32 bytes
		local BLOCK_START = Read(32)
		Value = {} :: any
		Value.Gadgets = {}
		local Elements_2 = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
		for _ = 1, Elements_2 do
			local OFFSET_0 = Read(1)
			local Key_2 = buffer.readu8(RecieveBuffer, OFFSET_0)
			local OFFSET_1 = Read(1)
			local Element_2 = (buffer.readu8(RecieveBuffer, OFFSET_1) == 1)
			Value.Gadgets[Key_2] = Element_2
		end
		Value.EquippedGadgets = {}
		Elements_2 = buffer.readu16(RecieveBuffer, BLOCK_START + 2)
		for _ = 1, Elements_2 do
			local OFFSET_0 = Read(1)
			local Key_2 = buffer.readu8(RecieveBuffer, OFFSET_0)
			local OFFSET_1 = Read(1)
			local Element_2 = buffer.readu8(RecieveBuffer, OFFSET_1)
			Value.EquippedGadgets[Key_2] = Element_2
		end
		Value.Emotes = {}
		Elements_2 = buffer.readu16(RecieveBuffer, BLOCK_START + 4)
		for _ = 1, Elements_2 do
			local OFFSET_0 = Read(1)
			local Key_2 = buffer.readu8(RecieveBuffer, OFFSET_0)
			local OFFSET_1 = Read(1)
			local Element_2 = (buffer.readu8(RecieveBuffer, OFFSET_1) == 1)
			Value.Emotes[Key_2] = Element_2
		end
		Value.EquippedEmotes = {}
		Elements_2 = buffer.readu16(RecieveBuffer, BLOCK_START + 6)
		for _ = 1, Elements_2 do
			local OFFSET_0 = Read(1)
			local Key_2 = buffer.readu8(RecieveBuffer, OFFSET_0)
			local OFFSET_1 = Read(1)
			local Element_2 = buffer.readu8(RecieveBuffer, OFFSET_1)
			Value.EquippedEmotes[Key_2] = Element_2
		end
		Value.Bits = buffer.readu32(RecieveBuffer, BLOCK_START + 8)
		Value.Wins = buffer.readu16(RecieveBuffer, BLOCK_START + 12)
		Value.FastestWin = buffer.readf64(RecieveBuffer, BLOCK_START + 14)
		Value.Level = buffer.readu16(RecieveBuffer, BLOCK_START + 22)
		Value.Xp = buffer.readu32(RecieveBuffer, BLOCK_START + 24)
		Value.GamepassDictionary = {}
		Elements_2 = buffer.readu16(RecieveBuffer, BLOCK_START + 28)
		for _ = 1, Elements_2 do
			local OFFSET_0 = Read(2)
			local Length = buffer.readu16(RecieveBuffer, OFFSET_0)
			local Key_2 = buffer.readstring(RecieveBuffer, Read(Length), Length)
			local OFFSET_1 = Read(1)
			local Element_2 = (buffer.readu8(RecieveBuffer, OFFSET_1) == 1)
			Value.GamepassDictionary[Key_2] = Element_2
		end
		Value.DeveloperProductPurchases = {}
		Elements_2 = buffer.readu16(RecieveBuffer, BLOCK_START + 30)
		for _ = 1, Elements_2 do
			local OFFSET_0 = Read(2)
			local Length = buffer.readu16(RecieveBuffer, OFFSET_0)
			local Key_2 = buffer.readstring(RecieveBuffer, Read(Length), Length)
			local OFFSET_1 = Read(2)
			local Element_2 = buffer.readu16(RecieveBuffer, OFFSET_1)
			Value.DeveloperProductPurchases[Key_2] = Element_2
		end
	end
	return Value
end

local function WriteEVENT_Enqueue(Value: Instance): ()
	-- Allocate BLOCK: 1 bytes
	local BLOCK_START = Allocate(1)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 3)
	table.insert(SendInstances, Value or false :: any)
end

local function WriteEVENT_Dequeue(Value: Instance): ()
	-- Allocate BLOCK: 1 bytes
	local BLOCK_START = Allocate(1)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 4)
	table.insert(SendInstances, Value or false :: any)
end

if not RunService:IsRunning() then
	local NOOP = function() end
	local Returns = table.freeze({
	FetchPlayerData = {
		Invoke = NOOP
	},
	PurchaseInventoryItem = {
		Invoke = NOOP
	},
	EquipItemAtIndex = {
		Invoke = NOOP
	},
	Enqueue = {
		Fire = NOOP
	},
	Dequeue = {
		Fire = NOOP
	},
	})
	return Returns :: BLINK_EVENTS_SYMBOL
end

if not RunService:IsClient() then
    error("Client network module can only be required from the client.")
end

local Reliable: RemoteEvent = ReplicatedStorage:WaitForChild("BLINK_RELIABLE_REMOTE") :: RemoteEvent
local Unreliable: UnreliableRemoteEvent = ReplicatedStorage:WaitForChild("BLINK_UNRELIABLE_REMOTE") :: UnreliableRemoteEvent

local function StepReplication()
    if SendCursor <= 0 then
        return
    end

    local Buffer = buffer.create(SendCursor)
    buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
    Reliable:FireServer(Buffer, SendInstances)

    SendSize = 64
    SendCursor = 0
    SendOffset = 0
    SendBuffer = buffer.create(64)
    table.clear(SendInstances)
end
local Elapsed = 0
RunService.Heartbeat:Connect(function(DeltaTime: number)
	Elapsed += DeltaTime
	if Elapsed >= (1 / 61) then
		Elapsed -= (1 / 61)
		StepReplication()
	end
end)
Reliable.OnClientEvent:Connect(function(Buffer: buffer, Instances: {Instance})
	RecieveCursor = 0
	RecieveBuffer = Buffer
	RecieveInstances = Instances
	RecieveInstanceCursor = 0
	local Size = buffer.len(RecieveBuffer)
	while (RecieveCursor < Size) do
		-- Read BLOCK: 1 bytes
		local BLOCK_START = Read(1)
		local Index = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
		if Index == 0 then
			-- Read BLOCK: 1 bytes
			local BLOCK_START = Read(1)
			local InvocationIdentifier = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
			if Calls[InvocationIdentifier] ~= nil then
				local Success, ReturnValue = pcall(function ()
					return ReadFUNCTION_RETURN_FetchPlayerData()
				end)
				task.spawn(Calls[InvocationIdentifier], Success, ReturnValue)
				Calls[InvocationIdentifier] = nil
			end
		elseif Index == 1 then
			-- Read BLOCK: 1 bytes
			local BLOCK_START = Read(1)
			local InvocationIdentifier = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
			if Calls[InvocationIdentifier] ~= nil then
				local Success, ReturnValue = pcall(function ()
					return ReadFUNCTION_RETURN_PurchaseInventoryItem()
				end)
				task.spawn(Calls[InvocationIdentifier], Success, ReturnValue)
				Calls[InvocationIdentifier] = nil
			end
		elseif Index == 2 then
			-- Read BLOCK: 1 bytes
			local BLOCK_START = Read(1)
			local InvocationIdentifier = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
			if Calls[InvocationIdentifier] ~= nil then
				local Success, ReturnValue = pcall(function ()
					return ReadFUNCTION_RETURN_EquipItemAtIndex()
				end)
				task.spawn(Calls[InvocationIdentifier], Success, ReturnValue)
				Calls[InvocationIdentifier] = nil
			end
		end
	end
end)
Unreliable.OnClientEvent:Connect(function(Buffer: buffer, Instances: {Instance})
	RecieveCursor = 0
	RecieveBuffer = Buffer
	RecieveInstances = Instances
	RecieveInstanceCursor = 0
	local Size = buffer.len(RecieveBuffer)
	while (RecieveCursor < Size) do
		-- Read BLOCK: 1 bytes
		local BLOCK_START = Read(1)
		local Index = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	end
end)


local Returns = table.freeze({
	StepReplication = StepReplication,

	FetchPlayerData = {
		Invoke = function(Value: nil): ({ Gadgets: {[number]: boolean}, EquippedGadgets: {[number]: number}, Emotes: {[number]: boolean}, EquippedEmotes: {[number]: number}, Bits: number, Wins: number, FastestWin: number, Level: number, Xp: number, GamepassDictionary: {[string]: boolean}, DeveloperProductPurchases: {[string]: number} }?)
			local InvocationIdentifier = Invoke()
			if Calls[InvocationIdentifier] ~= nil then
				Invocations -= 1
				error("More than 256 calls are awaiting a response, this packet has been dropped.")
			end
			WriteFUNCTION_DATA_FetchPlayerData(Value, InvocationIdentifier)
			Calls[InvocationIdentifier] = coroutine.running()
			local Success, ReturnValue: { Gadgets: {[number]: boolean}, EquippedGadgets: {[number]: number}, Emotes: {[number]: boolean}, EquippedEmotes: {[number]: number}, Bits: number, Wins: number, FastestWin: number, Level: number, Xp: number, GamepassDictionary: {[string]: boolean}, DeveloperProductPurchases: {[string]: number} }? = coroutine.yield()
			if Success ~= true then
				error("There was an exception while processing \"FetchPlayerData\".")
			end
			return ReturnValue
		end
	},
	PurchaseInventoryItem = {
		Invoke = function(Value1: number, Value2: string): ({ Gadgets: {[number]: boolean}, EquippedGadgets: {[number]: number}, Emotes: {[number]: boolean}, EquippedEmotes: {[number]: number}, Bits: number, Wins: number, FastestWin: number, Level: number, Xp: number, GamepassDictionary: {[string]: boolean}, DeveloperProductPurchases: {[string]: number} }?)
			local InvocationIdentifier = Invoke()
			if Calls[InvocationIdentifier] ~= nil then
				Invocations -= 1
				error("More than 256 calls are awaiting a response, this packet has been dropped.")
			end
			WriteFUNCTION_DATA_PurchaseInventoryItem(Value1, Value2, InvocationIdentifier)
			Calls[InvocationIdentifier] = coroutine.running()
			local Success, ReturnValue: { Gadgets: {[number]: boolean}, EquippedGadgets: {[number]: number}, Emotes: {[number]: boolean}, EquippedEmotes: {[number]: number}, Bits: number, Wins: number, FastestWin: number, Level: number, Xp: number, GamepassDictionary: {[string]: boolean}, DeveloperProductPurchases: {[string]: number} }? = coroutine.yield()
			if Success ~= true then
				error("There was an exception while processing \"PurchaseInventoryItem\".")
			end
			return ReturnValue
		end
	},
	EquipItemAtIndex = {
		Invoke = function(Value1: number, Value2: number, Value3: string): ({ Gadgets: {[number]: boolean}, EquippedGadgets: {[number]: number}, Emotes: {[number]: boolean}, EquippedEmotes: {[number]: number}, Bits: number, Wins: number, FastestWin: number, Level: number, Xp: number, GamepassDictionary: {[string]: boolean}, DeveloperProductPurchases: {[string]: number} }?)
			local InvocationIdentifier = Invoke()
			if Calls[InvocationIdentifier] ~= nil then
				Invocations -= 1
				error("More than 256 calls are awaiting a response, this packet has been dropped.")
			end
			WriteFUNCTION_DATA_EquipItemAtIndex(Value1, Value2, Value3, InvocationIdentifier)
			Calls[InvocationIdentifier] = coroutine.running()
			local Success, ReturnValue: { Gadgets: {[number]: boolean}, EquippedGadgets: {[number]: number}, Emotes: {[number]: boolean}, EquippedEmotes: {[number]: number}, Bits: number, Wins: number, FastestWin: number, Level: number, Xp: number, GamepassDictionary: {[string]: boolean}, DeveloperProductPurchases: {[string]: number} }? = coroutine.yield()
			if Success ~= true then
				error("There was an exception while processing \"EquipItemAtIndex\".")
			end
			return ReturnValue
		end
	},
	Enqueue = {
		Fire = function(Value: Instance): ()
			WriteEVENT_Enqueue(Value)
		end
	},
	Dequeue = {
		Fire = function(Value: Instance): ()
			WriteEVENT_Dequeue(Value)
		end
	},
})
type BLINK_EVENTS_SYMBOL = typeof(Returns)
return Returns :: BLINK_EVENTS_SYMBOL